using UnityEngine;
using System.Collections;

public class AllCharacterScene : MonoBehaviour {

	public GameObject playerPickCaptain;
	// Use this for initialization
	void Start () {
		playerPickCaptain.SetActive (true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
